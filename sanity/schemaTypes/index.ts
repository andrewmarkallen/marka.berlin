import {eventType} from './eventTypes'
import {artistType} from './artistTypes'
import {venueType} from './venueTypes'
import {cvType, experienceType} from './cvTypes'
import {meType} from './meTypes'

export const schemaTypes = [eventType, artistType, venueType, cvType, experienceType, meType]
