import {eventType} from './eventTypes'
import {artistType} from './artistTypes'
import {venueType} from './venueTypes'
import {meType} from './meTypes'
import {role, CV, education, language, contact} from './CV'

export const schemaTypes = [
  eventType,
  artistType,
  venueType,
  meType,
  language,
  role,
  CV,
  education,
  contact,
]
